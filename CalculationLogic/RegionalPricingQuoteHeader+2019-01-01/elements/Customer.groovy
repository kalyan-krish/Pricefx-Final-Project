/**
 * Using Customer Header Input to get the Currency of Customer
 *
 */

 if (quoteProcessor.isPostPhase()) {

     // Fetching CustomerId
     String CustomerId = quoteProcessor.quoteView.customerId

     return CustomerId

 }

